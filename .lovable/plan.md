## DIAGNOSE — current state

**lead_source storage**: free-text `text` column on `intros_booked` (and `intros_run`, `leads`). No FK. The canonical dropdown list is `LEAD_SOURCES` in `src/types/index.ts` — and **"Event" is already in that list** (line 21). It is NOT in `EXCLUDED_LEAD_SOURCES` in `src/lib/sa/leadsBooked.ts`, so Event already counts as self-generated by today's rule. No change to SGL definition needed.

**Surfaces that render the LEAD_SOURCES dropdown** (every one needs to handle Event → event picker):
1. `src/components/leads/BookIntroDialog.tsx` — booking from a lead row (no source select today; uses lead.source).
2. `src/components/dashboard/BookIntroSheet.tsx` — primary "Book Intro" sheet.
3. `src/components/dashboard/WalkInIntroSheet.tsx` — walk-in.
4. `src/components/IntroBookingEntry.tsx` — shift recap intro entry.
5. `src/features/pipeline/components/PipelineDialogs.tsx` — new + edit booking.
6. `src/components/myday/EditBookingDialog.tsx` — has its OWN hardcoded LEAD_SOURCES (drift). I'll switch it to the canonical list as part of this work.
7. `src/components/leads/AddLeadDialog.tsx` — creates a `leads` row with a source (Event allowed but no booking yet, so no event tagging at lead-create; tag happens when intro is booked).

**Booking record shape**: `intros_booked` has 67 columns; adding `event_id uuid nullable` is clean and matches the existing `vip_session_id` pattern.

**SGL reach map** (Event must stay consistent on each):
- `useSaLeads`, `useSaLeadsBooked`, `useSaAllBooked`, `useSaLeaderboard` (WIG leaderboard)
- `useSourcedLeadsToText` / `src/lib/sa/sourcedLeadsToText.ts`
- Pipeline lead-source filter
- `lib/studio-metrics.ts` source breakdowns
- `lib/metrics/sourceMembership.ts`
All read `lead_source` strings and apply the `EXCLUDED_LEAD_SOURCES` predicate. Since Event is not excluded, no changes needed in those files — Event will flow through automatically.

## BUILD

### Data
- Migration: `events` table — `id`, `name`, `event_date date`, `cost_cents int nullable`, `is_active bool default true`, `created_by`, `created_at`, `updated_at`. GRANTs + RLS (authenticated full access, service_role all).
- Migration: `ALTER TABLE intros_booked ADD COLUMN event_id uuid REFERENCES events(id)`.

### New shared component
- `src/components/events/EventPicker.tsx` — controlled component used inside any booking form when `lead_source === 'Event'`. Shows:
  - Select of ACTIVE events (name + formatted local date).
  - "+ Create new event" → inline mini-form (name + DatePicker, no cost). Saves to events table via Supabase, immediately selects.
  - Required when source is Event.
- `src/hooks/useEvents.ts` — React Query hook (list active events, create event, invalidation).

### Booking flow integration
Add `EventPicker` rendered conditionally when source = Event, with required validation before save, and persist `event_id` on the booking insert in:
- `BookIntroSheet.tsx`
- `WalkInIntroSheet.tsx`
- `PipelineDialogs.tsx` (new + edit booking)
- `IntroBookingEntry.tsx`
- `EditBookingDialog.tsx` (also swap hardcoded list → canonical `LEAD_SOURCES`)
- `BookIntroDialog.tsx` — if the lead's source is `Event`, show picker; otherwise inherit.

`AddLeadDialog`: when source = Event, no booking yet so no event_id; deferred until intro is booked (matches existing VIP behavior).

### Admin
- `src/components/admin/EventsAdminPanel.tsx` — added as a section on `/admin`. Lists events (active + past), edit name/date, set/edit cost (in dollars; stored as cents), toggle active.
- `src/components/admin/EventCohortPanel.tsx` — pick an event, render:
  - Header: name, date (local parse), cost (if set).
  - Cohort list: every `intros_booked` row where `event_id = X` (not soft-deleted). Each row uses the existing **PersonJourneyCard** (reused as-is) on click/expand.
  - Per row: booked / showed / no-showed / outcome / bought (buy_date) derived via existing `didIntroActuallyRun`, `isCloseRun`, `getRunSaleDate`.
  - Totals: `# booked · # showed · # bought`, plus cost. **No revenue, no ROI.**

### Scope guards
- No change to `EXCLUDED_LEAD_SOURCES` — Event remains self-generated automatically.
- No new journey/outcome/sale logic — all reads go through existing canonical helpers.
- No change to other sources, no new date helpers, all dates use `dateUtils` + America/Chicago.

## COHERENCE PROOF (will be produced at the end)
- Create Event inline from a booking → row in `events` + `event_id` set on the booking; second booking picks the same event → both visible in the cohort SELECT.
- `SELECT lead_source, count(*) FROM intros_booked WHERE event_id IS NOT NULL` shows only `Event`.
- SGL: SA who booked an Event-source intro shows +1 in `useSaLeads`/`useSaLeadsBooked` (Event not in EXCLUDED set).
- Cohort totals match: `SELECT count(*) FILTER (WHERE booking_status_canon='SHOWED')` etc. for tagged bookings = numbers shown in UI.
- No source other than Event is changed (sources list identical except for Event picker behavior).

## Confirm before I build
- Scope of "booking flow" = all 6 surfaces listed above (yes / pick a subset)?
- Events Admin lives as a new section on `/admin` (yes / different location)?
- Cost stored in **cents** (int) with admin entering dollars (recommended) — OK?
