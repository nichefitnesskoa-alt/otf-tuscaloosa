## Goal\n\nMake the **Coached** and **Closes** numbers tappable in both coach tables, so Koa can see exactly which members are behind every cell — and instantly spot why the WIG and Studio numbers disagree for the same coach.\n\n## What gets built\n\n### 1. New shared drill-down sheet — `CoachAttributionDrillDown.tsx`\n\nA bottom sheet (mobile-first, matches existing dialog patterns) that lists the actual intros behind a number.\n\nHeader\n- `{Coach} — {Coached | Closes} · {date range label}`\n- Subtitle: source tag — `WIG tab` or `Studio tab` — so when Koa opens the same coach from both tabs side-by-side it's obvious which is which.\n- Count badge: `{n} intros`\n\nBody — one row per intro\n- Member name + intro date (Central Time)\n- Lead source (Web, VIP Class, Friend, etc.)\n- Result chip: `SALE` (green), `Follow-Up` (amber), `No Show` (gray), `2nd Intro Sale` (green outline — credits the 1st coach via Total Journey), `VIP Intro` (excluded — gray italic)\n- Booking-status chip if relevant (`CANCELLED`, `DELETED_SOFT`)\n- Tap row → opens existing intro detail / lead sheet\n\nFooter — **the reconciliation strip** (this is the part that solves the \